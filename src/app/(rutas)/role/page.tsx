import { PageRole } from "@/app/components";

import { Metadata } from "next";
export const metadata: Metadata = {
  title:"ROLE"
};
export default function Page() {

  return (
    <PageRole/>
  );
}
